# PropScan v4

Property feature analysis platform with dynamic location search.

## Deploy
Push to GitHub → Render auto-deploys.
Set `ADMIN_PASS` env var in Render dashboard.

## Routes
- `/` — Landing page
- `/app` — Scanner application
- `/admin` — Admin dashboard

## API: see /api/docs after deployment
